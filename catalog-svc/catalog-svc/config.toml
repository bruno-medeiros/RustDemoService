[server]
host = "0.0.0.0"
port = 3030

# PostgreSQL connection (matches docker-compose.yml db service)
[postgres]
host = "localhost"
port = 5432
user = "postgres"
password = "mypassword"
database = "postgres"
