name: Build

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - name: Setup Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version-file: ".nvmrc"
          cache: "npm"
          cache-dependency-path: package-lock.json
      - name: Install Node dependencies
        run: npm ci
      - name: Setup just
        uses: taiki-e/install-action@just
      - name: Install non-Cargo deps
        run: just pre-requisites
      - name: Generate OpenAPI code (needs Rust first)
        run: just code-gen
      - name: Verify OpenAPI spec is up to date
        run: |
          git diff --exit-code catalog-svc/openapi.json  || (
            echo "catalog-svc/openapi.json is out of date. Run 'just generate-openapi' and commit the changes."
            exit 1
          )
      - name: Build
        run: cargo build
      - name: Lint
        run: cargo clippy --all-targets -- --deny warnings
      - name: Setup containers for tests
        run: |
          docker compose -f docker-compose.yml up -d
          for attempt in {1..60}; do
            if docker compose -f docker-compose.yml exec -T db pg_isready -U postgres -d postgres; then
              exit 0
            fi
            sleep 1
          done
          docker compose -f docker-compose.yml logs db
          exit 1
      - name: Run tests
        env:
          DATABASE_URL: "postgres://postgres:mypassword@127.0.0.1:5432/postgres"
        run: cargo test
      - name: Build frontend
        run: just build-frontend

#  docker:
#    name: Build-Push Docker
#    runs-on: ubuntu-latest
#    needs: build
#    if: github.event_name != 'pull_request'
#
#    steps:
#    - name: List files in the repository
#      run: |
#        ls ${{ github.workspace }}
#        echo "----"
#        ls .

      - name: docker login
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: bdom/rust-demo-service-temp

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64
          # Persist BuildKit layer cache between runs (cargo chef cook + build layers).
          # Note: --mount=type=cache content is not persisted; only image layers are.
          cache-from: type=gha
          cache-to: type=gha,mode=max


